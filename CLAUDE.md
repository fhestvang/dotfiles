# Claude Adapter

@AGENTS.md

`AGENTS.md` is the canonical repo contract for dotfiles. Keep Claude-specific
runtime defaults in `~/github/fhh-toolkit/runtimes/claude/`; dotfiles should
only call the sync helper during install.
